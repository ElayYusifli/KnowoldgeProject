# WIDOCO Documentation Output

Generate the ontology documentation into this folder:

```bash
java -jar widoco.jar -ontFile ../ontology/course-recommendation.ttl -outFolder . -rewriteAll -webVowl
```

After generation, publish this folder with GitHub Pages and place the public URL in the project report and final presentation.

